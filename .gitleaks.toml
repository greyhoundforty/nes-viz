# gitleaks configuration
# Docs: https://github.com/gitleaks/gitleaks?tab=readme-ov-file#configuration
title = "nes-viz"

[extend]
# Use the default ruleset as a base
useDefault = true

# Add project-specific allowlist entries here if you hit false positives.
# Example:
# [[rules.allowlist]]
# description = "Test fixture — not a real key"
# regexes = ["AKIA_FAKE_KEY_FOR_TESTING"]
